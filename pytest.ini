[pytest]
# The repo root contains code.py (the CircuitPython MagTag app), which shadows
# Python's stdlib `code` module. pytest's debugging (pdb) plugin does
# `import code` at startup and crashes on the CircuitPython file. We don't need
# interactive debugging for these test runs, so disable that plugin.
addopts = -p no:debugging
testpaths = tests
